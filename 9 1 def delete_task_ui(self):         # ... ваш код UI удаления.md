## Реализация метода `delete_task_ui()`

```python
def delete_task_ui(self):
    """Пользовательский интерфейс для удаления задачи"""
    print("\n" + "="*40)
    print("🗑️  УДАЛЕНИЕ ЗАДАЧИ")
    print("="*40)
    
    # Проверяем есть ли задачи для удаления
    if not self.manager.tasks:
        print("📭 Нет задач для удаления")
        return
    
    # Показываем все задачи для выбора
    self.display_tasks()
    
    # Выбор задачи для удаления
    try:
        task_id = int(input("\nВведите ID задачи для удаления: ").strip())
    except ValueError:
        print("❌ Некорректный ID! Должно быть число.")
        return
    
    # Находим задачу
    task = self.manager.get_task(task_id)
    if not task:
        print(f"❌ Задача с ID {task_id} не найдена!")
        return
    
    # Показываем информацию о задаче для подтверждения
    print(f"\n📋 ВЫ УДАЛЯЕТЕ ЗАДАЧУ:")
    print(f"   ID: {task.id}")
    print(f"   📝 {task.title}")
    print(f"   📄 {task.description}")
    print(f"   📊 {task.status.value}")
    print(f"   ⚡ {task.priority.value}")
    print(f"   📅 Создана: {task.created_date.strftime('%d.%m.%Y %H:%M')}")
    
    # Подтверждение удаления
    print("\n⚠️  Это действие нельзя отменить!")
    confirm = input("❓ Вы уверены, что хотите удалить задачу? (y/n): ").strip().lower()
    
    if confirm in ['y', 'yes', 'д', 'да']:
        if self.manager.delete_task(task_id):
            print(f"✅ Задача '{task.title}' успешно удалена!")
        else:
            print("❌ Ошибка при удалении задачи!")
    else:
        print("✅ Удаление отменено")
```

## 📝 Подробное объяснение:

### **Что делает метод:**
1. **Проверяет наличие задач** для удаления
2. **Показывает список задач** для удобного выбора
3. **Находит задачу по ID** с проверкой существования
4. **Показывает детали задачи** для подтверждения
5. **Запрашивает подтверждение** с предупреждением
6. **Выполняет удаление** через менеджер
7. **Дает обратную связь** о результате

### **Логика безопасности:**

| Шаг | Защита | Цель |
|-----|--------|------|
| Проверка задач | `if not self.manager.tasks:` | Предотвратить ошибку при пустом списке |
| Валидация ID | `try/except ValueError` | Обработать некорректный ввод |
| Поиск задачи | `if not task:` | Убедиться что задача существует |
| Подтверждение | Двойной запрос | Предотвратить случайное удаление |

---

## 🔧 Альтернативные варианты реализации:

### **Вариант 1: Удаление по названию**
```python
def delete_task_ui(self):
    """Удаление с поиском по названию"""
    print("\n🗑️  УДАЛЕНИЕ ЗАДАЧИ")
    
    if not self.manager.tasks:
        print("📭 Нет задач для удаления")
        return
    
    # Поиск по названию
    search_term = input("Введите название задачи для поиска: ").strip().lower()
    if not search_term:
        print("❌ Пустой поисковый запрос!")
        return
    
    # Поиск задач
    matching_tasks = [task for task in self.manager.tasks 
                     if search_term in task.title.lower()]
    
    if not matching_tasks:
        print(f"❌ Задачи с названием '{search_term}' не найдены")
        return
    
    # Показываем найденные задачи
    print(f"\n📋 НАЙДЕНО ЗАДАЧ: {len(matching_tasks)}")
    for task in matching_tasks:
        status_icon = "🆕" if task.status == Status.NEW else "🔄" if task.status == Status.IN_PROGRESS else "✅"
        print(f"  {status_icon} [{task.id}] {task.title}")
    
    # Выбор ID для удаления
    try:
        task_id = int(input("\nВведите ID задачи для удаления: ").strip())
    except ValueError:
        print("❌ Некорректный ID!")
        return
    
    # Проверяем что выбранная задача есть в найденных
    task_to_delete = next((task for task in matching_tasks if task.id == task_id), None)
    if not task_to_delete:
        print(f"❌ Задача с ID {task_id} не найдена в результатах поиска!")
        return
    
    # Подтверждение
    print(f"\n⚠️  УДАЛЕНИЕ: '{task_to_delete.title}'")
    confirm = input("❓ Подтвердите удаление (y/n): ").strip().lower()
    
    if confirm in ['y', 'yes', 'д', 'да']:
        if self.manager.delete_task(task_id):
            print("✅ Задача удалена!")
        else:
            print("❌ Ошибка удаления!")
    else:
        print("✅ Удаление отменено")
```

### **Вариант 2: Массовое удаление**
```python
def delete_task_ui(self):
    """Массовое удаление задач"""
    print("\n🗑️  МАССОВОЕ УДАЛЕНИЕ ЗАДАЧ")
    
    if not self.manager.tasks:
        print("📭 Нет задач для удаления")
        return
    
    # Показываем задачи с возможностью выбора нескольких
    self.display_tasks()
    
    print("\n🎯 Варианты удаления:")
    print("1. Удалить одну задачу по ID")
    print("2. Удалить все завершенные задачи")
    print("3. Удалить все задачи")
    print("4. Отмена")
    
    choice = input("Выберите действие (1-4): ").strip()
    
    if choice == '1':
        self._delete_single_task()
    elif choice == '2':
        self._delete_completed_tasks()
    elif choice == '3':
        self._delete_all_tasks()
    elif choice == '4':
        print("✅ Отменено")
    else:
        print("❌ Некорректный выбор!")

def _delete_single_task(self):
    """Удаление одной задачи"""
    try:
        task_id = int(input("Введите ID задачи для удаления: ").strip())
    except ValueError:
        print("❌ Некорректный ID!")
        return
    
    task = self.manager.get_task(task_id)
    if not task:
        print(f"❌ Задача с ID {task_id} не найдена!")
        return
    
    print(f"\n⚠️  Удаление: '{task.title}'")
    confirm = input("Подтвердите (y/n): ").strip().lower()
    
    if confirm in ['y', 'yes', 'д', 'да']:
        if self.manager.delete_task(task_id):
            print("✅ Задача удалена!")
        else:
            print("❌ Ошибка удаления!")

def _delete_completed_tasks(self):
    """Удаление всех завершенных задач"""
    completed_tasks = [task for task in self.manager.tasks 
                      if task.status == Status.COMPLETED]
    
    if not completed_tasks:
        print("📭 Нет завершенных задач для удаления")
        return
    
    print(f"\n📋 Найдено завершенных задач: {len(completed_tasks)}")
    for task in completed_tasks:
        print(f"  ✅ {task.title}")
    
    confirm = input(f"\n❓ Удалить {len(completed_tasks)} завершенных задач? (y/n): ").strip().lower()
    
    if confirm in ['y', 'yes', 'д', 'да']:
        deleted_count = 0
        for task in completed_tasks:
            if self.manager.delete_task(task.id):
                deleted_count += 1
        
        print(f"✅ Удалено {deleted_count} задач!")

def _delete_all_tasks(self):
    """Удаление всех задач"""
    if not self.manager.tasks:
        print("📭 Нет задач для удаления")
        return
    
    print(f"\n⚠️  ВНИМАНИЕ: Вы собираетесь удалить ВСЕ задачи!")
    print(f"📋 Всего задач: {len(self.manager.tasks)}")
    
    confirm1 = input("❓ Вы уверены? (y/n): ").strip().lower()
    if confirm1 not in ['y', 'yes', 'д', 'да']:
        print("✅ Отменено")
        return
    
    # Двойное подтверждение для опасной операции
    confirm2 = input("❓ ТОЧНО уверены? Это нельзя отменить! (y/n): ").strip().lower()
    if confirm2 in ['y', 'yes', 'д', 'да']:
        # Создаем резервную копию
        backup_tasks = self.manager.tasks.copy()
        
        # Удаляем все задачи
        self.manager.tasks.clear()
        self.manager.next_id = 1
        
        if self.manager.save_tasks():
            print(f"✅ Все задачи удалены!")
        else:
            # Восстанавливаем из резервной копии при ошибке
            self.manager.tasks = backup_tasks
            print("❌ Ошибка удаления! Задачи восстановлены.")
    else:
        print("✅ Отменено")
```

### **Вариант 3: Быстрое удаление**
```python
def delete_task_ui(self):
    """Быстрое удаление без лишних подтверждений"""
    print("\n🗑️  БЫСТРОЕ УДАЛЕНИЕ")
    
    if not self.manager.tasks:
        print("📭 Нет задач для удаления")
        return
    
    # Показываем только ID и названия
    print("\n📋 ДОСТУПНЫЕ ЗАДАЧИ:")
    for task in self.manager.tasks:
        status_icon = "🆕" if task.status == Status.NEW else "🔄" if task.status == Status.IN_PROGRESS else "✅"
        print(f"  {status_icon} [{task.id}] {task.title}")
    
    try:
        task_id = int(input("\nВведите ID задачи для удаления: ").strip())
    except ValueError:
        print("❌ Некорректный ID!")
        return
    
    task = self.manager.get_task(task_id)
    if not task:
        print(f"❌ Задача с ID {task_id} не найдена!")
        return
    
    # Минимальное подтверждение
    confirm = input(f"Удалить '{task.title}'? (y/n): ").strip().lower()
    
    if confirm in ['y', 'yes', 'д', 'да']:
        if self.manager.delete_task(task_id):
            print("✅ Удалено!")
        else:
            print("❌ Ошибка!")
    else:
        print("✅ Отменено")
```

---

## 🎯 Интеграция с менеджером задач:

```python
def delete_task(self, task_id):
    """Удаляет задачу (должен быть реализован в TaskManager)"""
    task = self.get_task(task_id)
    if task:
        self.tasks.remove(task)
        self.save_tasks()
        return True
    return False
```

---

## 🧪 Пример работы:

```
========================================
🗑️  УДАЛЕНИЕ ЗАДАЧИ
========================================

📋 НАЙДЕНО ЗАДАЧ: 3
================================================================================
#1
  ID: 1
  📝 Изучить Python
  📄 Освоить основы программирования
  🔄 в работе
  🔴 высокий
  📅 Создана: 15.01.2024 14:30
  🔄 Обновлена: 15.01.2024 14:30
--------------------------------------------------------------------------------

Введите ID задачи для удаления: 1

📋 ВЫ УДАЛЯЕТЕ ЗАДАЧУ:
   ID: 1
   📝 Изучить Python
   📄 Освоить основы программирования
   📊 в работе
   ⚡ высокий
   📅 Создана: 15.01.2024 14:30

⚠️  Это действие нельзя отменить!
❓ Вы уверены, что хотите удалить задачу? (y/n): y
✅ Задача 'Изучить Python' успешно удалена!
```

---

## 💡 Рекомендация:

**Используйте улучшенную базовую версию с двойным подтверждением:**

```python
def delete_task_ui(self):
    """Пользовательский интерфейс для удаления задачи"""
    print("\n🗑️  УДАЛЕНИЕ ЗАДАЧИ")
    
    if not self.manager.tasks:
        print("📭 Нет задач для удаления")
        return
    
    self.display_tasks()
    
    try:
        task_id = int(input("\nВведите ID задачи для удаления: ").strip())
    except ValueError:
        print("❌ Некорректный ID!")
        return
    
    task = self.manager.get_task(task_id)
    if not task:
        print(f"❌ Задача с ID {task_id} не найдена!")
        return
    
    # Показываем детали задачи
    print(f"\n📋 ВЫБРАНА ЗАДАЧА:")
    print(f"   ID: {task.id}")
    print(f"   📝 {task.title}")
    print(f"   📄 {task.description}")
    print(f"   📊 Статус: {task.status.value}")
    print(f"   ⚡ Приоритет: {task.priority.value}")
    
    # Двойное подтверждение для важных задач
    if task.priority == Priority.HIGH or task.status == Status.IN_PROGRESS:
        print("\n⚠️  ВНИМАНИЕ: Это важная или активная задача!")
    
    confirm = input("\n❓ Вы уверены, что хотите удалить эту задачу? (y/n): ").strip().lower()
    
    if confirm in ['y', 'yes', 'д', 'да']:
        # Финальное подтверждение
        final_confirm = input("❓ ТОЧНО уверены? Это действие нельзя отменить! (y/n): ").strip().lower()
        
        if final_confirm in ['y', 'yes', 'д', 'да']:
            if self.manager.delete_task(task_id):
                print(f"✅ Задача '{task.title}' удалена!")
            else:
                print("❌ Ошибка при удалении!")
        else:
            print("✅ Удаление отменено")
    else:
        print("✅ Удаление отменено")
```

Эта версия:
- ✅ Проверяет наличие задач перед удалением
- ✅ Показывает список для выбора
- ✅ Валидирует ввод ID
- ✅ Показывает детали удаляемой задачи
- ✅ Использует двойное подтверждение для безопасности
- ✅ Особое внимание для важных/активных задач
- ✅ Дает четкую обратную связь о результате
