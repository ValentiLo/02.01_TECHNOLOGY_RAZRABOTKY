## Реализация метода `edit_task_ui()`

```python
def edit_task_ui(self):
    """Пользовательский интерфейс для редактирования задачи"""
    print("\n" + "="*40)
    print("✏️  РЕДАКТИРОВАНИЕ ЗАДАЧИ")
    print("="*40)
    
    # Проверяем есть ли задачи для редактирования
    if not self.manager.tasks:
        print("📭 Нет задач для редактирования")
        return
    
    # Показываем все задачи для выбора
    self.display_tasks()
    
    # Выбор задачи для редактирования
    try:
        task_id = int(input("\nВведите ID задачи для редактирования: ").strip())
    except ValueError:
        print("❌ Некорректный ID! Должно быть число.")
        return
    
    # Находим задачу
    task = self.manager.get_task(task_id)
    if not task:
        print(f"❌ Задача с ID {task_id} не найдена!")
        return
    
    print(f"\n📝 Редактирование задачи: '{task.title}'")
    print("Оставьте поле пустым, чтобы не изменять значение")
    print("-" * 50)
    
    # Редактирование заголовка
    new_title = input(f"Новый заголовок [{task.title}]: ").strip()
    
    # Редактирование описания
    new_description = input(f"Новое описание [{task.description}]: ").strip()
    
    # Редактирование статуса
    print("\n📊 Выберите новый статус:")
    print("1. 🆕 Новая")
    print("2. 🔄 В работе")
    print("3. ✅ Завершена")
    status_choice = input(f"Выбор [текущий: {task.status.value}]: ").strip()
    
    # Редактирование приоритета
    print("\n🎯 Выберите новый приоритет:")
    print("1. 🔴 Высокий")
    print("2. 🟡 Средний")
    print("3. ⚪ Низкий")
    priority_choice = input(f"Выбор [текущий: {task.priority.value}]: ").strip()
    
    # Собираем изменения
    updates = {}
    
    if new_title:
        updates['title'] = new_title
    
    if new_description:
        updates['description'] = new_description
    
    if status_choice:
        status_map = {'1': Status.NEW, '2': Status.IN_PROGRESS, '3': Status.COMPLETED}
        if status_choice in status_map:
            updates['status'] = status_map[status_choice]
    
    if priority_choice:
        priority_map = {'1': Priority.HIGH, '2': Priority.MEDIUM, '3': Priority.LOW}
        if priority_choice in priority_map:
            updates['priority'] = priority_map[priority_choice]
    
    # Применяем изменения
    if updates:
        if self.manager.update_task(task_id, **updates):
            print(f"\n✅ Задача успешно обновлена!")
            
            # Показываем обновленную задачу
            updated_task = self.manager.get_task(task_id)
            if updated_task:
                print("\n📋 ОБНОВЛЕННАЯ ЗАДАЧА:")
                print(f"   Заголовок: {updated_task.title}")
                print(f"   Описание: {updated_task.description}")
                print(f"   Статус: {updated_task.status.value}")
                print(f"   Приоритет: {updated_task.priority.value}")
        else:
            print("❌ Ошибка при обновлении задачи!")
    else:
        print("ℹ️  Не было внесено изменений")
```

## 📝 Подробное объяснение:

### **Что делает метод:**
1. **Проверяет наличие задач** для редактирования
2. **Показывает список задач** для удобного выбора
3. **Находит задачу по ID** с проверкой существования
4. **Предлагает редактирование полей** с текущими значениями
5. **Собирает изменения** только для заполненных полей
6. **Применяет обновления** через менеджер
7. **Показывает результат** с обновленными данными

### **Логика редактирования:**

| Поле | Поведение при пустом вводе | Пример |
|------|----------------------------|---------|
| Заголовок | Не изменяется | "[Старый заголовок]" |
| Описание | Не изменяется | "[Старое описание]" |
| Статус | Не изменяется | "Выбор [текущий: в работе]" |
| Приоритет | Не изменяется | "Выбор [текущий: высокий]" |

---

## 🔧 Альтернативные варианты реализации:

### **Вариант 1: Пошаговое редактирование**
```python
def edit_task_ui(self):
    """Пошаговое редактирование с меню выбора полей"""
    print("\n✏️  РЕДАКТИРОВАНИЕ ЗАДАЧИ")
    
    if not self.manager.tasks:
        print("📭 Нет задач для редактирования")
        return
    
    # Выбор задачи
    try:
        task_id = int(input("Введите ID задачи для редактирования: ").strip())
    except ValueError:
        print("❌ Некорректный ID!")
        return
    
    task = self.manager.get_task(task_id)
    if not task:
        print(f"❌ Задача с ID {task_id} не найдена!")
        return
    
    print(f"\n📝 Редактирование: '{task.title}'")
    
    while True:
        print("\n🎯 Что вы хотите изменить?")
        print("1. 📝 Заголовок")
        print("2. 📄 Описание")
        print("3. 📊 Статус")
        print("4. ⚡ Приоритет")
        print("5. ✅ Завершить редактирование")
        print("6. ❌ Отменить")
        
        choice = input("Ваш выбор (1-6): ").strip()
        
        if choice == '1':
            new_title = input(f"Новый заголовок [{task.title}]: ").strip()
            if new_title:
                self.manager.update_task(task_id, title=new_title)
                print("✅ Заголовок обновлен")
        
        elif choice == '2':
            new_description = input(f"Новое описание [{task.description}]: ").strip()
            if new_description:
                self.manager.update_task(task_id, description=new_description)
                print("✅ Описание обновлено")
        
        elif choice == '3':
            print("\n📊 Выберите статус:")
            print("1. 🆕 Новая")
            print("2. 🔄 В работе")
            print("3. ✅ Завершена")
            status_choice = input(f"Выбор [текущий: {task.status.value}]: ").strip()
            if status_choice in ['1', '2', '3']:
                status_map = {'1': Status.NEW, '2': Status.IN_PROGRESS, '3': Status.COMPLETED}
                self.manager.update_task(task_id, status=status_map[status_choice])
                print("✅ Статус обновлен")
        
        elif choice == '4':
            print("\n🎯 Выберите приоритет:")
            print("1. 🔴 Высокий")
            print("2. 🟡 Средний")
            print("3. ⚪ Низкий")
            priority_choice = input(f"Выбор [текущий: {task.priority.value}]: ").strip()
            if priority_choice in ['1', '2', '3']:
                priority_map = {'1': Priority.HIGH, '2': Priority.MEDIUM, '3': Priority.LOW}
                self.manager.update_task(task_id, priority=priority_map[priority_choice])
                print("✅ Приоритет обновлен")
        
        elif choice == '5':
            print("✅ Редактирование завершено")
            break
        
        elif choice == '6':
            print("❌ Редактирование отменено")
            return
        
        else:
            print("❌ Некорректный выбор!")
        
        # Обновляем объект задачи для отображения актуальных данных
        task = self.manager.get_task(task_id)
```

### **Вариант 2: Быстрое редактирование статуса**
```python
def edit_task_ui(self):
    """Быстрое редактирование только статуса задачи"""
    print("\n🔄 БЫСТРОЕ ИЗМЕНЕНИЕ СТАТУСА")
    
    if not self.manager.tasks:
        print("📭 Нет задач для редактирования")
        return
    
    # Показываем только ID и заголовки для быстрого выбора
    print("\n📋 ДОСТУПНЫЕ ЗАДАЧИ:")
    for task in self.manager.tasks:
        status_icon = "🆕" if task.status == Status.NEW else "🔄" if task.status == Status.IN_PROGRESS else "✅"
        print(f"  {status_icon} [{task.id}] {task.title}")
    
    try:
        task_id = int(input("\nВведите ID задачи: ").strip())
    except ValueError:
        print("❌ Некорректный ID!")
        return
    
    task = self.manager.get_task(task_id)
    if not task:
        print(f"❌ Задача с ID {task_id} не найдена!")
        return
    
    print(f"\n📝 Задача: '{task.title}'")
    print(f"📊 Текущий статус: {task.status.value}")
    
    print("\n🎯 Выберите новый статус:")
    print("1. 🆕 Новая")
    print("2. 🔄 В работе") 
    print("3. ✅ Завершена")
    
    status_choice = input("Ваш выбор (1-3): ").strip()
    status_map = {'1': Status.NEW, '2': Status.IN_PROGRESS, '3': Status.COMPLETED}
    
    if status_choice in status_map:
        new_status = status_map[status_choice]
        if self.manager.update_task(task_id, status=new_status):
            print(f"✅ Статус изменен на: {new_status.value}")
        else:
            print("❌ Ошибка при изменении статуса!")
    else:
        print("❌ Некорректный выбор!")
```

### **Вариант 3: Редактирование с предпросмотром**
```python
def edit_task_ui(self):
    """Редактирование с предпросмотром изменений"""
    print("\n✏️  РЕДАКТИРОВАНИЕ ЗАДАЧИ")
    
    if not self.manager.tasks:
        print("📭 Нет задач для редактирования")
        return
    
    self.display_tasks()
    
    try:
        task_id = int(input("\nВведите ID задачи для редактирования: ").strip())
    except ValueError:
        print("❌ Некорректный ID!")
        return
    
    task = self.manager.get_task(task_id)
    if not task:
        print(f"❌ Задача с ID {task_id} не найдена!")
        return
    
    print(f"\n📝 Редактирование задачи:")
    print(f"   Текущий заголовок: {task.title}")
    print(f"   Текущее описание: {task.description}")
    print(f"   Текущий статус: {task.status.value}")
    print(f"   Текущий приоритет: {task.priority.value}")
    print("-" * 50)
    
    # Собираем новые значения
    new_title = input("Новый заголовок (Enter - оставить текущий): ").strip()
    new_description = input("Новое описание (Enter - оставить текущий): ").strip()
    
    print("\nНовый статус:")
    print("1. 🆕 Новая | 2. 🔄 В работе | 3. ✅ Завершена")
    status_choice = input("Выбор (Enter - оставить текущий): ").strip()
    
    print("\nНовый приоритет:")
    print("1. 🔴 Высокий | 2. 🟡 Средний | 3. ⚪ Низкий")
    priority_choice = input("Выбор (Enter - оставить текущий): ").strip()
    
    # Показываем предпросмотр изменений
    print("\n📋 ПРЕДПРОСМОТР ИЗМЕНЕНИЙ:")
    print(f"   Заголовок: {new_title if new_title else task.title} {'(изменено)' if new_title else ''}")
    print(f"   Описание: {new_description if new_description else task.description} {'(изменено)' if new_description else ''}")
    
    status_map = {'1': Status.NEW, '2': Status.IN_PROGRESS, '3': Status.COMPLETED}
    new_status = status_map[status_choice] if status_choice in status_map else task.status
    print(f"   Статус: {new_status.value} {'(изменено)' if status_choice else ''}")
    
    priority_map = {'1': Priority.HIGH, '2': Priority.MEDIUM, '3': Priority.LOW}
    new_priority = priority_map[priority_choice] if priority_choice in priority_map else task.priority
    print(f"   Приоритет: {new_priority.value} {'(изменено)' if priority_choice else ''}")
    
    # Подтверждение
    confirm = input("\n✅ Применить изменения? (y/n): ").strip().lower()
    if confirm in ['y', 'yes', 'д', 'да']:
        updates = {}
        if new_title: updates['title'] = new_title
        if new_description: updates['description'] = new_description
        if status_choice: updates['status'] = new_status
        if priority_choice: updates['priority'] = new_priority
        
        if updates and self.manager.update_task(task_id, **updates):
            print("✅ Изменения применены!")
        else:
            print("ℹ️  Не было изменений или ошибка применения")
    else:
        print("❌ Изменения отменены")
```

---

## 🎯 Интеграция с менеджером задач:

```python
def update_task(self, task_id, **kwargs):
    """Обновляет задачу (должен быть реализован в TaskManager)"""
    task = self.get_task(task_id)
    if task:
        for key, value in kwargs.items():
            if hasattr(task, key):
                setattr(task, key, value)
        task.updated_date = datetime.now()
        self.save_tasks()
        return True
    return False
```

---

## 🧪 Пример работы:

```
========================================
✏️  РЕДАКТИРОВАНИЕ ЗАДАЧИ
========================================

📋 НАЙДЕНО ЗАДАЧ: 3
================================================================================
#1
  ID: 1
  📝 Изучить Python
  📄 Освоить основы программирования
  🔄 в работе
  🔴 высокий
  📅 Создана: 15.01.2024 14:30
  🔄 Обновлена: 15.01.2024 14:30
--------------------------------------------------------------------------------

Введите ID задачи для редактирования: 1

📝 Редактирование задачи: 'Изучить Python'
Оставьте поле пустым, чтобы не изменять значение
--------------------------------------------------
Новый заголовок [Изучить Python]: Изучить Python углубленно

Новое описание [Освоить основы программирования]: Изучить ООП и фреймворки

📊 Выберите новый статус:
1. 🆕 Новая
2. 🔄 В работе  
3. ✅ Завершена
Выбор [текущий: в работе]: 3

🎯 Выберите новый приоритет:
1. 🔴 Высокий
2. 🟡 Средний
3. ⚪ Низкий
Выбор [текущий: высокий]: 

✅ Задача успешно обновлена!

📋 ОБНОВЛЕННАЯ ЗАДАЧА:
   Заголовок: Изучить Python углубленно
   Описание: Изучить ООП и фреймворки
   Статус: завершена
   Приоритет: высокий
```

---

## 💡 Рекомендация:

**Используйте базовую версию с улучшенной обработкой ошибок:**

```python
def edit_task_ui(self):
    """Пользовательский интерфейс для редактирования задачи"""
    print("\n✏️  РЕДАКТИРОВАНИЕ ЗАДАЧИ")
    
    if not self.manager.tasks:
        print("📭 Нет задач для редактирования")
        return
    
    self.display_tasks()
    
    try:
        task_id = int(input("\nВведите ID задачи для редактирования: ").strip())
    except ValueError:
        print("❌ Некорректный ID! Должно быть число.")
        return
    
    task = self.manager.get_task(task_id)
    if not task:
        print(f"❌ Задача с ID {task_id} не найдена!")
        return
    
    print(f"\n📝 Редактирование: '{task.title}'")
    print("(оставьте поле пустым чтобы не менять)")
    
    # Собираем обновления
    updates = {}
    
    new_title = input(f"Новый заголовок [{task.title}]: ").strip()
    if new_title:
        updates['title'] = new_title
    
    new_description = input(f"Новое описание [{task.description}]: ").strip()
    if new_description:
        updates['description'] = new_description
    
    print("\n📊 Статус:")
    print("1.🆕 2.🔄 3.✅")
    status_choice = input(f"Выбор [текущий: {task.status.value}]: ").strip()
    if status_choice in ['1', '2', '3']:
        updates['status'] = [Status.NEW, Status.IN_PROGRESS, Status.COMPLETED][int(status_choice)-1]
    
    print("\n🎯 Приоритет:")
    print("1.🔴 2.🟡 3.⚪")
    priority_choice = input(f"Выбор [текущий: {task.priority.value}]: ").strip()
    if priority_choice in ['1', '2', '3']:
        updates['priority'] = [Priority.HIGH, Priority.MEDIUM, Priority.LOW][int(priority_choice)-1]
    
    # Применяем изменения
    if updates:
        if self.manager.update_task(task_id, **updates):
            print("✅ Задача обновлена!")
        else:
            print("❌ Ошибка обновления!")
    else:
        print("ℹ️  Изменений нет")
```

Эта версия:
- ✅ Проверяет наличие задач
- ✅ Показывает список для выбора
- ✅ Валидирует ID задачи
- ✅ Позволяет редактировать отдельные поля
- ✅ Сохраняет текущие значения по умолчанию
- ✅ Дает обратную связь о результате
