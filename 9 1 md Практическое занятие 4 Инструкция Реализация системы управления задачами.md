**Менеджер задач нужно делать как отдельную программу!** 

Вот подробное объяснение:

## 🎯 Правильный подход:

### 1. **Создайте отдельный файл**
- Создайте файл `task_manager.py`
- Скопируйте туда весь код из предыдущего ответа
- Сохраните и запускайте как самостоятельную программу

### 2. **Как запускать:**
```bash
# В командной строке перейдите в папку с проектом
cd путь_к_вашей_папке

# Запустите программу
python task_manager.py
```

### 3. **Структура файла будет такой:**
```python
# task_manager.py
from datetime import datetime
from enum import Enum
import json

class Status(Enum):
    # ... код перечислений

class Priority(Enum):
    # ... код перечислений

class Task:
    # ... код класса Task

class TaskManager:
    # ... код менеджера задач

class TaskManagerUI:
    # ... код интерфейса

def main():
    # ... код запуска

if __name__ == "__main__":
    main()
```

## ❌ Чего НЕ делать:

**НЕ смешивайте** код менеджера задач с другими программами или заданиями. Каждая практическая работа должна быть в отдельном файле.

## 📁 Рекомендуемая структура папки:

```
папка_студента/
├── task_manager.py          # Программа менеджера задач
├── tasks.json              # Файл с данными (создастся автоматически)
└── README.md               # Отчет о работе (если требуется)
```

## 🚀 Пошаговая инструкция запуска:

1. **Создайте новый файл** в текстовом редакторе
2. **Сохраните как** `task_manager.py`
3. **Скопируйте весь код** системы управления задачами
4. **Откройте командную строку/терминал**
5. **Перейдите в папку** с файлом:
   ```bash
   cd C:\Users\ВашеИмя\Documents\project_folder
   ```
6. **Запустите программу:**
   ```bash
   python task_manager.py
   ```

## 🔧 Если возникают проблемы с запуском:

**Проверьте:**
- Установлен ли Python (`python --version`)
- Правильно ли сохранен файл (расширение `.py`)
- Нет ли синтаксических ошибок в коде
- Все ли классы и функции реализованы

## 💡 Пример минимальной проверки:

Создайте тестовый файл `test.py`:
```python
print("Python работает корректно!")
```
Запустите: `python test.py` - если видите надпись, значит среда настроена правильно.

# Инструкция по выполнению практической работы №4
## «Реализация системы управления задачами»

---

## 📋 Подготовительный этап

### Шаг 1: Настройка среды разработки
1. **Установите Python** (если не установлен)
   - Скачайте с официального сайта python.org
   - Версия 3.6 или выше
   - Проверьте установку: в командной строке выполните `python --version`

2. **Создайте рабочую папку**
   ```bash
   mkdir task_manager_project
   cd task_manager_project
   ```

3. **Создайте файл проекта**
   - Создайте файл с именем `task_manager.py`
   - Откройте его в любом текстовом редакторе или IDE

---

## 🏗️ Этап разработки

### Шаг 2: Создание базовой структуры проекта
**В файле `task_manager.py` последовательно добавляйте код:**

1. **Импортируйте необходимые модули**
   ```python
   from datetime import datetime
   from enum import Enum
   import json
   ```

2. **Создайте перечисления для статусов и приоритетов**
   ```python
   class Status(Enum):
       NEW = "новая"
       IN_PROGRESS = "в работе"
       COMPLETED = "завершена"

   class Priority(Enum):
       LOW = "низкий"
       MEDIUM = "средний"
       HIGH = "высокий"
   ```

### Шаг 3: Реализация класса Task
```python
class Task:
    def __init__(self, id, title, description, status=Status.NEW, priority=Priority.MEDIUM):
        self.id = id
        self.title = title
        self.description = description
        self.status = status
        self.priority = priority
        self.created_date = datetime.now()
        self.updated_date = datetime.now()
    
    def to_dict(self):
        # Реализуйте метод для преобразования в словарь
    
    @classmethod
    def from_dict(cls, data):
        # Реализуйте метод для создания объекта из словаря
```

**Задание:** Реализуйте методы `to_dict()` и `from_dict()` согласно примеру из полного кода.

### Шаг 4: Реализация класса TaskManager
```python
class TaskManager:
    def __init__(self, filename='tasks.json'):
        self.filename = filename
        self.tasks = []
        self.next_id = 1
        self.load_tasks()
    
    def load_tasks(self):
        # Реализуйте загрузку из файла
    
    def save_tasks(self):
        # Реализуйте сохранение в файл
    
    def add_task(self, title, description, priority=Priority.MEDIUM):
        # Реализуйте добавление задачи
```

**Задание:** Постепенно реализуйте все методы класса TaskManager:
- `get_task()`
- `update_task()`
- `delete_task()`
- `get_all_tasks()`
- `get_tasks_by_status()`
- `get_tasks_by_priority()`

### Шаг 5: Создание пользовательского интерфейса
```python
class TaskManagerUI:
    def __init__(self):
        self.manager = TaskManager()
    
    def display_menu(self):
        # Реализуйте отображение меню
    
    def display_tasks(self, tasks=None):
        # Реализуйте отображение списка задач
```

**Задание:** Реализуйте все методы UI по одному, тестируя каждый:
- `add_task_ui()`
- `edit_task_ui()`
- `delete_task_ui()`
- `filter_by_status_ui()`
- `filter_by_priority_ui()`
- `run()` - главный цикл

### Шаг 6: Добавление точки входа
```python
def main():
    try:
        app = TaskManagerUI()
        app.run()
    except Exception as e:
        print(f"Критическая ошибка: {e}")

if __name__ == "__main__":
    main()
```

---

## 🧪 Этап тестирования

### Шаг 7: Пошаговое тестирование функциональности

1. **Запустите программу**
   ```bash
   python task_manager.py
   ```

2. **Протестируйте каждую функцию:**
   - ✅ **Добавление задачи**: создайте 3-4 тестовые задачи
   - ✅ **Просмотр всех задач**: убедитесь, что задачи отображаются
   - ✅ **Редактирование**: измените статус одной из задач
   - ✅ **Удаление**: удалите одну задачу
   - ✅ **Фильтрация**: проверьте фильтры по статусу и приоритету

3. **Проверьте сохранение данных:**
   - Закройте программу
   - Запустите снова
   - Убедитесь, что задачи сохранились

---

## 📊 Этап анализа

### Шаг 8: Анализ реализованной логики

**Ответьте на вопросы:**
1. Какие принципы ООП использованы в проекте?
2. Как организовано хранение данных?
3. Какие исключительные ситуации обрабатываются?
4. Как можно улучшить архитектуру приложения?

---

## 📝 Отчет о выполнении работы

### Шаг 9: Оформление отчета

**Создайте файл `README.md` с содержанием:**

```markdown
# Отчет по практической работе №4
## Система управления задачами

### Выполненные задачи:
- [x] Реализована модель данных
- [x] Создана бизнес-логика приложения
- [x] Реализован пользовательский интерфейс
- [x] Организовано хранение данных
- [x] Протестирована функциональность

### Архитектура проекта:
- Класс Task - модель задачи
- Класс TaskManager - бизнес-логика
- Класс TaskManagerUI - пользовательский интерфейс

### Использованные технологии:
- Python 3.x
- JSON для хранения данных
- ООП принципы
```

---

## 🚀 Дополнительные задания (по желанию)

### Шаг 10: Усовершенствование системы

**Варианты улучшений:**
1. Добавить категории задач
2. Реализовать поиск по ключевым словам
3. Добавить сроки выполнения задач
4. Создать графический интерфейс (Tkinter)
5. Реализовать экспорт задач в CSV

---

## ⏱️ Ориентировочное время выполнения

- **Шаги 1-6**: 2-3 часа
- **Шаг 7 (тестирование)**: 30 минут
- **Шаги 8-9 (анализ и отчет)**: 30 минут
- **Итого**: 3-4 часа

---

## ❗ Важные замечания

1. **Сохраняйте код регулярно** после реализации каждой функции
2. **Тестируйте каждую функцию** перед переходом к следующей
3. **В случае ошибок** - внимательно читайте сообщения об ошибках
4. **Используйте отладку** - выводите промежуточные результаты
5. **Следуйте стилю кода** - используйте понятные названия переменных и функций

Удачи в выполнении практической работы! 🎯



# 📋 def load_tasks(self):
    📋    # Реализуйте загрузку из файла

## Реализация метода `load_tasks`

```python
def load_tasks(self):
    """Загрузка задач из JSON файла"""
    try:
        # Пытаемся открыть файл для чтения
        with open(self.filename, 'r', encoding='utf-8') as file:
            # Загружаем данные из JSON
            tasks_data = json.load(file)
            
            # Преобразуем словари обратно в объекты Task
            self.tasks = []
            for task_dict in tasks_data:
                task = Task.from_dict(task_dict)
                self.tasks.append(task)
            
            # Определяем следующий ID для новых задач
            if self.tasks:
                self.next_id = max(task.id for task in self.tasks) + 1
            else:
                self.next_id = 1
                
        print(f"Загружено {len(self.tasks)} задач из файла {self.filename}")
        
    except FileNotFoundError:
        # Если файл не существует - создаем пустой список задач
        self.tasks = []
        self.next_id = 1
        print(f"Файл {self.filename} не найден. Создан новый список задач.")
        
    except json.JSONDecodeError:
        # Если файл поврежден или пустой
        self.tasks = []
        self.next_id = 1
        print(f"Ошибка чтения файла {self.filename}. Создан новый список задач.")
        
    except Exception as e:
        # Обработка любых других ошибок
        self.tasks = []
        self.next_id = 1
        print(f"Неожиданная ошибка при загрузке задач: {e}")
```

## 📝 Пояснение к коду:

### **Что делает метод:**
1. **Пытается открыть файл** с задачами
2. **Читает JSON данные** и преобразует их в объекты Task
3. **Восстанавливает next_id** для автоматической нумерации новых задач
4. **Обрабатывает ошибки** если файл не существует или поврежден

### **Обработка ошибок:**
- `FileNotFoundError` - файл не существует (первый запуск)
- `JSONDecodeError` - файл поврежден или пустой
- `Exception` - любые другие неожиданные ошибки

### **Логика работы next_id:**
- Если есть задачи: `max_id + 1`
- Если задач нет: начинаем с 1

---

## 🔧 Дополнительные улучшения (опционально):

### **Версия с более детальным логированием:**
```python
def load_tasks(self):
    """Загрузка задач из JSON файла с детальным логированием"""
    try:
        with open(self.filename, 'r', encoding='utf-8') as file:
            tasks_data = json.load(file)
            
            self.tasks = []
            loaded_count = 0
            
            for task_dict in tasks_data:
                try:
                    task = Task.from_dict(task_dict)
                    self.tasks.append(task)
                    loaded_count += 1
                except Exception as e:
                    print(f"Ошибка загрузки задачи: {e}")
                    continue
            
            if self.tasks:
                self.next_id = max(task.id for task in self.tasks) + 1
            else:
                self.next_id = 1
                
            print(f"Успешно загружено {loaded_count} из {len(tasks_data)} задач")
            
    except FileNotFoundError:
        self.tasks = []
        self.next_id = 1
        print("Файл не найден. Инициализирован пустой список задач.")
        
    except json.JSONDecodeError as e:
        self.tasks = []
        self.next_id = 1
        print(f"Ошибка формата JSON: {e}. Инициализирован новый список задач.")
        
    except Exception as e:
        self.tasks = []
        self.next_id = 1
        print(f"Критическая ошибка: {e}. Программа продолжит с пустым списком задач.")
```

### **Проверка целостности данных:**
```python
def load_tasks(self):
    """Загрузка с проверкой целостности данных"""
    try:
        with open(self.filename, 'r', encoding='utf-8') as file:
            tasks_data = json.load(file)
            
            if not isinstance(tasks_data, list):
                raise ValueError("Некорректный формат данных в файле")
            
            self.tasks = []
            for task_dict in tasks_data:
                # Проверяем обязательные поля
                required_fields = ['id', 'title', 'description', 'status', 'priority']
                if all(field in task_dict for field in required_fields):
                    task = Task.from_dict(task_dict)
                    self.tasks.append(task)
                else:
                    print("Пропущена задача с отсутствующими полями")
            
            # Сортируем задачи по ID для consistency
            self.tasks.sort(key=lambda x: x.id)
            
            if self.tasks:
                self.next_id = max(task.id for task in self.tasks) + 1
            else:
                self.next_id = 1
                
    except FileNotFoundError:
        self.tasks = []
        self.next_id = 1
    except Exception as e:
        print(f"Ошибка загрузки: {e}")
        self.tasks = []
        self.next_id = 1
```

**Рекомендую использовать первую (базовую) версию** - она покрывает все основные случаи и достаточно проста для понимания.


# def to_dict(self):
        # Реализуйте метод для преобразования в словарь

## Реализация метода `to_dict()`

```python
def to_dict(self):
    """Преобразует объект Task в словарь для сохранения в JSON"""
    return {
        'id': self.id,
        'title': self.title,
        'description': self.description,
        'status': self.status.value,  # Берем значение из Enum
        'priority': self.priority.value,  # Берем значение из Enum
        'created_date': self.created_date.isoformat(),  # Преобразуем дату в строку
        'updated_date': self.updated_date.isoformat()   # Преобразуем дату в строку
    }
```

## 📝 Подробное объяснение:

### **Что делает метод:**
Преобразует все свойства объекта Task в простой словарь, который можно сохранить в JSON файл.

### **Почему нужен этот метод:**
- JSON не понимает Python объекты, Enum и datetime
- Нужно преобразовать сложные типы в простые (строки, числа)
- Для последующего сохранения в файл

### **По каждому полю:**

| Поле | Тип в объекте | Тип в словаре | Преобразование |
|------|---------------|---------------|----------------|
| `id` | `int` | `int` | Без изменений |
| `title` | `str` | `str` | Без изменений |
| `description` | `str` | `str` | Без изменений |
| `status` | `Status enum` | `str` | `status.value` |
| `priority` | `Priority enum` | `str` | `priority.value` |
| `created_date` | `datetime` | `str` | `isoformat()` |
| `updated_date` | `datetime` | `str` | `isoformat()` |

---

## 🔧 Альтернативные варианты реализации:

### **Вариант 1: С проверкой на None**
```python
def to_dict(self):
    """С защитой от None значений"""
    return {
        'id': self.id,
        'title': self.title if self.title else "",
        'description': self.description if self.description else "",
        'status': self.status.value if self.status else Status.NEW.value,
        'priority': self.priority.value if self.priority else Priority.MEDIUM.value,
        'created_date': self.created_date.isoformat() if self.created_date else datetime.now().isoformat(),
        'updated_date': self.updated_date.isoformat() if self.updated_date else datetime.now().isoformat()
    }
```

### **Вариант 2: С дополнительными полями**
```python
def to_dict(self):
    """Расширенная версия с дополнительной информацией"""
    task_dict = {
        'id': self.id,
        'title': self.title,
        'description': self.description,
        'status': self.status.value,
        'priority': self.priority.value,
        'created_date': self.created_date.isoformat(),
        'updated_date': self.updated_date.isoformat(),
        # Дополнительные вычисляемые поля
        'is_completed': self.status == Status.COMPLETED,
        'is_high_priority': self.priority == Priority.HIGH,
        'days_since_created': (datetime.now() - self.created_date).days
    }
    return task_dict
```

### **Вариант 3: Используя dataclasses (для Python 3.7+)**
```python
from dataclasses import dataclass, asdict
from datetime import datetime
from enum import Enum

@dataclass
class Task:
    id: int
    title: str
    description: str
    status: Status = Status.NEW
    priority: Priority = Priority.MEDIUM
    created_date: datetime = None
    updated_date: datetime = None
    
    def __post_init__(self):
        if self.created_date is None:
            self.created_date = datetime.now()
        if self.updated_date is None:
            self.updated_date = datetime.now()
    
    def to_dict(self):
        """Используем встроенную функцию dataclasses"""
        task_dict = asdict(self)
        # Преобразуем Enum и datetime вручную
        task_dict['status'] = self.status.value
        task_dict['priority'] = self.priority.value
        task_dict['created_date'] = self.created_date.isoformat()
        task_dict['updated_date'] = self.updated_date.isoformat()
        return task_dict
```

---

## 🧪 Пример использования:

```python
# Создаем задачу
task = Task(
    id=1,
    title="Изучить Python",
    description="Освоить основы программирования",
    status=Status.IN_PROGRESS,
    priority=Priority.HIGH
)

# Преобразуем в словарь
task_dict = task.to_dict()
print(task_dict)
```

**Результат:**
```python
{
    'id': 1,
    'title': 'Изучить Python',
    'description': 'Освоить основы программирования',
    'status': 'в работе',
    'priority': 'высокий',
    'created_date': '2024-01-15T10:30:00.123456',
    'updated_date': '2024-01-15T10:30:00.123456'
}
```

---

## 💡 Рекомендация:

**Используйте базовую версию** - она простая, понятная и покрывает все потребности для этой практической работы.

```python
def to_dict(self):
    return {
        'id': self.id,
        'title': self.title,
        'description': self.description,
        'status': self.status.value,
        'priority': self.priority.value,
        'created_date': self.created_date.isoformat(),
        'updated_date': self.updated_date.isoformat()
    }
```

Этот метод будет корректно работать с методом `save_tasks()`, который использует `json.dump()`.
        



# @classmethod
    def from_dict(cls, data):
        # Реализуйте метод для создания объекта из словаря


## Реализация метода `from_dict()`

```python
@classmethod
def from_dict(cls, data):
    """Создает объект Task из словаря (обратное преобразование)"""
    # Создаем базовый объект без сложных типов
    task = cls(
        id=data['id'],
        title=data['title'],
        description=data['description']
    )
    
    # Восстанавливаем статус из строки в Enum
    task.status = Status(data['status'])
    
    # Восстанавливаем приоритет из строки в Enum
    task.priority = Priority(data['priority'])
    
    # Восстанавливаем даты из строк в datetime объекты
    task.created_date = datetime.fromisoformat(data['created_date'])
    task.updated_date = datetime.fromisoformat(data['updated_date'])
    
    return task
```

## 📝 Подробное объяснение:

### **Что делает метод:**
Преобразует словарь (из JSON файла) обратно в полноценный объект Task с правильными типами данных.

### **Почему это classmethod:**
- `@classmethod` позволяет создавать объекты через класс, а не через экземпляр
- Первый параметр `cls` - ссылка на класс, а не на объект
- Можно вызывать как `Task.from_dict(data)`

### **Процесс восстановления:**

| Поле в словаре | Тип в словаре | Тип в объекте | Преобразование |
|----------------|---------------|---------------|----------------|
| `id` | `int` | `int` | Без изменений |
| `title` | `str` | `str` | Без изменений |
| `description` | `str` | `str` | Без изменений |
| `status` | `str` | `Status enum` | `Status(data['status'])` |
| `priority` | `str` | `Priority enum` | `Priority(data['priority'])` |
| `created_date` | `str` | `datetime` | `fromisoformat()` |
| `updated_date` | `str` | `datetime` | `fromisoformat()` |

---

## 🔧 Альтернативные варианты реализации:

### **Вариант 1: С обработкой ошибок**
```python
@classmethod
def from_dict(cls, data):
    """С защитой от некорректных данных"""
    try:
        task = cls(
            id=data.get('id', 0),
            title=data.get('title', ''),
            description=data.get('description', '')
        )
        
        # Восстанавливаем статус с fallback значением
        try:
            task.status = Status(data['status'])
        except (KeyError, ValueError):
            task.status = Status.NEW
            
        # Восстанавливаем приоритет с fallback значением
        try:
            task.priority = Priority(data['priority'])
        except (KeyError, ValueError):
            task.priority = Priority.MEDIUM
            
        # Восстанавливаем даты с fallback значением
        try:
            task.created_date = datetime.fromisoformat(data['created_date'])
        except (KeyError, ValueError):
            task.created_date = datetime.now()
            
        try:
            task.updated_date = datetime.fromisoformat(data['updated_date'])
        except (KeyError, ValueError):
            task.updated_date = datetime.now()
            
        return task
        
    except Exception as e:
        print(f"Ошибка создания задачи из данных: {e}")
        # Возвращаем задачу по умолчанию
        return cls(0, "Восстановленная задача", "Задача была восстановлена после ошибки")
```

### **Вариант 2: Компактная версия**
```python
@classmethod
def from_dict(cls, data):
    """Компактная версия с обработкой по умолчанию"""
    return cls(
        id=data.get('id', 0),
        title=data.get('title', 'Без названия'),
        description=data.get('description', ''),
        status=Status(data.get('status', Status.NEW.value)),
        priority=Priority(data.get('priority', Priority.MEDIUM.value)),
        created_date=datetime.fromisoformat(data.get('created_date', datetime.now().isoformat())),
        updated_date=datetime.fromisoformat(data.get('updated_date', datetime.now().isoformat()))
    )
```

### **Вариант 3: С валидацией данных**
```python
@classmethod
def from_dict(cls, data):
    """С проверкой обязательных полей"""
    # Проверяем обязательные поля
    required_fields = ['id', 'title', 'description', 'status', 'priority', 'created_date', 'updated_date']
    missing_fields = [field for field in required_fields if field not in data]
    
    if missing_fields:
        raise ValueError(f"Отсутствуют обязательные поля: {missing_fields}")
    
    # Создаем и возвращаем объект
    task = cls(
        id=data['id'],
        title=data['title'],
        description=data['description']
    )
    task.status = Status(data['status'])
    task.priority = Priority(data['priority'])
    task.created_date = datetime.fromisoformat(data['created_date'])
    task.updated_date = datetime.fromisoformat(data['updated_date'])
    
    return task
```

---

## 🧪 Пример использования:

```python
# Данные из JSON файла
task_data = {
    'id': 1,
    'title': 'Изучить Python',
    'description': 'Освоить основы программирования',
    'status': 'в работе',
    'priority': 'высокий',
    'created_date': '2024-01-15T10:30:00.123456',
    'updated_date': '2024-01-15T11:45:00.654321'
}

# Восстанавливаем объект Task
task = Task.from_dict(task_data)

print(f"ID: {task.id}")
print(f"Заголовок: {task.title}")
print(f"Статус: {task.status}")  # Status.IN_PROGRESS
print(f"Приоритет: {task.priority}")  # Priority.HIGH
print(f"Создана: {task.created_date}")  # datetime объект
```

---

## 🔄 Полный цикл работы:

```python
# 1. Создаем задачу
task = Task(1, "Тест", "Описание")

# 2. Сохраняем в словарь (для JSON)
task_dict = task.to_dict()
# {'id': 1, 'title': 'Тест', 'status': 'новая', ...}

# 3. Сохраняем в файл
import json
with open('tasks.json', 'w') as f:
    json.dump([task_dict], f)

# 4. Загружаем из файла
with open('tasks.json', 'r') as f:
    loaded_data = json.load(f)

# 5. Восстанавливаем объект
restored_task = Task.from_dict(loaded_data[0])
```

---

## 💡 Рекомендация:

**Используйте базовую версию с небольшим улучшением обработки ошибок:**

```python
@classmethod
def from_dict(cls, data):
    """Создает объект Task из словаря"""
    task = cls(
        id=data['id'],
        title=data['title'],
        description=data['description']
    )
    
    # Восстанавливаем сложные типы данных
    task.status = Status(data['status'])
    task.priority = Priority(data['priority'])
    task.created_date = datetime.fromisoformat(data['created_date'])
    task.updated_date = datetime.fromisoformat(data['updated_date'])
    
    return task
```

Этот метод будет идеально работать в паре с `to_dict()` для полного цикла сохранения/загрузки задач.
