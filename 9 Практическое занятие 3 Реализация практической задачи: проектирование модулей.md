### **Практическое занятие 3: Проектирование модулей**
**МДК.02.01 Технология разработки программного обеспечения**

---

## **Цель занятия:**
Освоить практические навыки проектирования модулей программной системы, включая разработку API, проектирование базы данных и создание технической документации.

---

## **Теоретическая часть**

### **1. Принципы модульного проектирования**

**Ключевые принципы:**
- **Принцип единственной ответственности (SRP)** - каждый модуль отвечает за одну функцию
- **Слабая связанность** - минимизация зависимостей между модулями
- **Высокая связность** - тесная взаимосвязь элементов внутри модуля
- **Инкапсуляция** - сокрытие внутренней реализации

### **2. Подходы к проектированию модулей**

- **Модульно-ориентированное проектирование**
- **Объектно-ориентированное проектирование**
- **Функционально-ориентированное проектирование**
- **Сервис-ориентированная архитектура (SOA)**

---

## **Практическая часть**

### **Проект: Система управления складом "SmartStorage"**

**Контекст:** Компания логистики нуждается в системе управления складскими запасами для автоматизации учета товаров.

---

## **Этап 1: Выделение модулей системы**

### **Модульная структура системы:**

```
SmartStorage System
├── 🛡️ Модуль аутентификации и авторизации
├── 📦 Модуль управления товарами
├── 📋 Модуль управления категориями
├── 🏷️ Модуль управления поставщиками
├── 📊 Модуль отчетности и аналитики
├── 🔔 Модуль уведомлений
└── ⚙️ Модуль системных настроек
```

---

## **Этап 2: Детальное проектирование модулей**

### **Модуль 1: Аутентификация и авторизация**

**Назначение:** Управление пользователями и правами доступа

**Функциональность:**
- Регистрация и аутентификация пользователей
- Управление ролями и правами
- Восстановление пароля
- Ведение лога авторизации

**API endpoints:**
```python
# auth_module.py
class AuthModule:
    POST /api/auth/register
    POST /api/auth/login
    POST /api/auth/logout
    POST /api/auth/refresh-token
    POST /api/auth/reset-password
    GET  /api/auth/profile
```

**Модель данных:**
```sql
-- Таблица пользователей
CREATE TABLE users (
    id SERIAL PRIMARY KEY,
    email VARCHAR(255) UNIQUE NOT NULL,
    password_hash VARCHAR(255) NOT NULL,
    full_name VARCHAR(100),
    role_id INTEGER REFERENCES roles(id),
    is_active BOOLEAN DEFAULT true,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- Таблица ролей
CREATE TABLE roles (
    id SERIAL PRIMARY KEY,
    name VARCHAR(50) UNIQUE NOT NULL,
    permissions JSONB
);
```

### **Модуль 2: Управление товарами**

**Назначение:** Учет и управление складскими позициями

**Функциональность:**
- Добавление/редактирование/удаление товаров
- Поиск и фильтрация товаров
- Управление остатками
- История изменений

**API endpoints:**
```python
# products_module.py
class ProductsModule:
    GET    /api/products              # Список товаров
    POST   /api/products              # Создание товара
    GET    /api/products/{id}         # Получение товара
    PUT    /api/products/{id}         # Обновление товара
    DELETE /api/products/{id}         # Удаление товара
    GET    /api/products/search       # Поиск товаров
    POST   /api/products/{id}/stock   # Изменение остатка
```

**Модель данных:**
```sql
-- Таблица товаров
CREATE TABLE products (
    id SERIAL PRIMARY KEY,
    sku VARCHAR(50) UNIQUE NOT NULL,
    name VARCHAR(255) NOT NULL,
    description TEXT,
    category_id INTEGER REFERENCES categories(id),
    supplier_id INTEGER REFERENCES suppliers(id),
    unit_price DECIMAL(10,2),
    current_stock INTEGER DEFAULT 0,
    min_stock_level INTEGER DEFAULT 0,
    max_stock_level INTEGER,
    barcode VARCHAR(100),
    is_active BOOLEAN DEFAULT true,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- Таблица движения товаров
CREATE TABLE stock_movements (
    id SERIAL PRIMARY KEY,
    product_id INTEGER REFERENCES products(id),
    movement_type VARCHAR(20), -- 'in', 'out', 'adjustment'
    quantity INTEGER NOT NULL,
    reason VARCHAR(255),
    user_id INTEGER REFERENCES users(id),
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

### **Модуль 3: Управление категориями**

**Назначение:** Организация товаров в иерархическую структуру

**Функциональность:**
- Создание древовидной структуры категорий
- Привязка товаров к категориям
- Валидация структуры категорий

**API endpoints:**
```python
# categories_module.py
class CategoriesModule:
    GET    /api/categories
    POST   /api/categories
    GET    /api/categories/{id}
    PUT    /api/categories/{id}
    DELETE /api/categories/{id}
    GET    /api/categories/{id}/products
```

**Модель данных:**
```sql
-- Таблица категорий
CREATE TABLE categories (
    id SERIAL PRIMARY KEY,
    name VARCHAR(255) NOT NULL,
    description TEXT,
    parent_id INTEGER REFERENCES categories(id),
    sort_order INTEGER DEFAULT 0,
    is_active BOOLEAN DEFAULT true,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

### **Модуль 4: Управление поставщиками**

**Назначение:** Ведение базы поставщиков и контактной информации

**Функциональность:**
- Учет поставщиков
- История поставок
- Контактная информация

**API endpoints:**
```python
# suppliers_module.py
class SuppliersModule:
    GET    /api/suppliers
    POST   /api/suppliers
    GET    /api/suppliers/{id}
    PUT    /api/suppliers/{id}
    DELETE /api/suppliers/{id}
    GET    /api/suppliers/{id}/products
```

---

## **Этап 3: Проектирование интерфейсов модулей**

### **Интерфейс модуля товаров:**
```python
from abc import ABC, abstractmethod
from typing import List, Optional, Dict
from datetime import datetime

class IProductService(ABC):
    @abstractmethod
    def create_product(self, product_data: Dict) -> Dict:
        pass
    
    @abstractmethod
    def get_product(self, product_id: int) -> Optional[Dict]:
        pass
    
    @abstractmethod
    def update_product(self, product_id: int, update_data: Dict) -> bool:
        pass
    
    @abstractmethod
    def delete_product(self, product_id: int) -> bool:
        pass
    
    @abstractmethod
    def search_products(self, filters: Dict) -> List[Dict]:
        pass
    
    @abstractmethod
    def update_stock(self, product_id: int, quantity: int, reason: str) -> bool:
        pass

class IProductRepository(ABC):
    @abstractmethod
    def save(self, product_data: Dict) -> Dict:
        pass
    
    @abstractmethod
    def find_by_id(self, product_id: int) -> Optional[Dict]:
        pass
    
    @abstractmethod
    def update(self, product_id: int, update_data: Dict) -> bool:
        pass
    
    @abstractmethod
    def delete(self, product_id: int) -> bool:
        pass
    
    @abstractmethod
    def find_by_filters(self, filters: Dict) -> List[Dict]:
        pass
```

### **Реализация сервиса товаров:**
```python
class ProductService(IProductService):
    def __init__(self, product_repo: IProductRepository, stock_repo: IStockRepository):
        self.product_repo = product_repo
        self.stock_repo = stock_repo
    
    def create_product(self, product_data: Dict) -> Dict:
        # Валидация данных
        if not self._validate_product_data(product_data):
            raise ValueError("Invalid product data")
        
        # Генерация SKU
        if not product_data.get('sku'):
            product_data['sku'] = self._generate_sku(product_data['name'])
        
        # Сохранение товара
        return self.product_repo.save(product_data)
    
    def update_stock(self, product_id: int, quantity: int, reason: str) -> bool:
        product = self.product_repo.find_by_id(product_id)
        if not product:
            raise ValueError(f"Product {product_id} not found")
        
        # Обновление остатка
        new_stock = product['current_stock'] + quantity
        if new_stock < 0:
            raise ValueError("Insufficient stock")
        
        update_data = {'current_stock': new_stock}
        self.product_repo.update(product_id, update_data)
        
        # Запись в историю движений
        movement_data = {
            'product_id': product_id,
            'movement_type': 'in' if quantity > 0 else 'out',
            'quantity': abs(quantity),
            'reason': reason
        }
        self.stock_repo.record_movement(movement_data)
        
        return True
    
    def _validate_product_data(self, product_data: Dict) -> bool:
        required_fields = ['name', 'category_id']
        return all(field in product_data for field in required_fields)
    
    def _generate_sku(self, product_name: str) -> str:
        # Простая генерация SKU на основе названия
        prefix = product_name[:3].upper()
        timestamp = datetime.now().strftime("%m%d%H%M")
        return f"{prefix}-{timestamp}"
```

---

## **Этап 4: Проектирование взаимодействия модулей**

### **Схема взаимодействия:**
```
┌─────────────────┐    ┌──────────────────┐    ┌─────────────────┐
│   Web Client    │    │   API Gateway    │    │   Auth Module   │
│                 │───▶│                  │───▶│                 │
└─────────────────┘    └──────────────────┘    └─────────────────┘
                              │
                              ▼
┌─────────────────┐    ┌──────────────────┐    ┌─────────────────┐
│  Products       │    │   Categories     │    │   Suppliers     │
│  Module         │◄──▶│   Module         │    │   Module        │
└─────────────────┘    └──────────────────┘    └─────────────────┘
         │                        │
         ▼                        ▼
┌─────────────────┐    ┌──────────────────┐
│   Database      │    │  Notifications   │
│   Layer         │    │   Module         │
└─────────────────┘    └──────────────────┘
```

### **Пример последовательности операций:**
```python
# sequence_diagram.py
def add_new_product_flow():
    # 1. Аутентификация пользователя
    auth_token = auth_module.authenticate(user_credentials)
    
    # 2. Проверка прав доступа
    if not auth_module.has_permission(auth_token, 'products:create'):
        raise PermissionError("Insufficient permissions")
    
    # 3. Проверка существования категории
    category = categories_module.get_category(category_id)
    if not category:
        raise ValueError("Category not found")
    
    # 4. Создание товара
    new_product = products_module.create_product(product_data)
    
    # 5. Отправка уведомления
    notifications_module.send_notification(
        f"New product created: {new_product['name']}"
    )
    
    return new_product
```

---

## **Практическое задание для студентов**

### **Задание: Проектирование модулей для системы "Библиотека"**

**Контекст:** Разработка системы управления городской библиотекой

**Требуется спроектировать:**
1. Модуль управления книгами
2. Модуль управления читателями
3. Модуль выдачи и возврата книг
4. Модуль учета задолженностей

**Шаблон для выполнения:**

```markdown
# Проект: Библиотечная система

## 1. Модульная структура
- [Опишите модули системы]

## 2. Детальное проектирование модулей

### Модуль управления книгами
**API endpoints:**
```
[ваши endpoints]
```

**Модель данных:**
```sql
[SQL таблицы]
```

**Интерфейс сервиса:**
```python
[интерфейс на Python]
```

## 3. Взаимодействие модулей
[Опишите как модули взаимодействуют]
```

### **Пример решения для модуля книг:**

**API endpoints:**
```python
class BooksModule:
    GET    /api/books                 # Список книг
    POST   /api/books                 # Добавление книги
    GET    /api/books/{id}            # Получение книги
    PUT    /api/books/{id}            # Обновление книги
    DELETE /api/books/{id}            # Удаление книги
    GET    /api/books/search          # Поиск книг
    GET    /api/books/{id}/availability # Проверка доступности
```

---

## **Контрольные вопросы**

1. Какие принципы следует соблюдать при проектировании модулей?
2. Как обеспечить слабую связанность между модулями?
3. Что такое интерфейс модуля и зачем он нужен?
4. Как организовать взаимодействие между модулями?
5. Какие подходы к проектированию API вы знаете?
6. Как обеспечить целостность данных при работе нескольких модулей?
7. Что такое dependency injection и как он помогает в модульном дизайне?
8. Как тестировать отдельные модули системы?

---

## **Критерии оценки**

**Отлично (5):**
- Полное и логичное разделение на модули
- Детальное проектирование API и моделей данных
- Правильное применение принципов модульности
- Глубокое понимание взаимодействия модулей

**Хорошо (4):**
- Корректное выделение основных модулей
- Наличие основных API endpoints
- Незначительные недочеты в проектировании

**Удовлетворительно (3):**
- Поверхностное проектирование модулей
- Отсутствие ключевых элементов проектирования
- Нарушение принципов модульности

---

**Вывод:** В ходе практического занятия были освоены ключевые навыки проектирования модулей программной системы. Правильное модульное проектирование позволяет создавать масштабируемые, поддерживаемые и тестируемые программные продукты, что является основой профессиональной разработки программного обеспечения.
